# UnLock VPN — Лог сессии 2026-07-24

> Тема: высокий пинг. Добавлены два новых сервера (🇩🇪 Германия + 🇷🇺 Москва) и построена
> **каскадная система** 🇷🇺→🇩🇪 (домашний вход в Москве → выход во Франкфурте).
> Секреты — только по расположению (в `.env`, `~/.ssh/…`, `/root/*-pass.txt`), значения не вписаны.
> Правила: Claude пишет весь код сам; коммитит, но НЕ пушит.

---

## 0. Итог сессии

| # | Что | Статус |
|---|-----|--------|
| 1 | Диагноз пинга: MTS душит зарубежные endpoints (ICMP зарезан, TCP-handshake подделан локально) | ✅ понято |
| 2 | 🇩🇪 Франкфурт как **Marzban-нода** (VLESS Reality :2096 + Hysteria2 :443) | ✅ live, egress DE |
| 3 | 🇷🇺 Москва как **DNAT-релей** (tcp/2096 + udp/443 → Франкфурт) | ✅ live |
| 4 | Каскад 🇷🇺→🇩🇪 в подписке (VLESS + Hy2), синтез в шлюзе | ✅ проверено вживую |
| 5 | Порядок подписки: Каскад → 🇩🇪 → 🇵🇱 → 🇳🇱 → 🇺🇸 | ✅ на проде |
| 6 | Оба сервера: SSH только по ключу (пароли из чата обезврежены) | ✅ |
| 7 | Автопродление LE-серта на DE (nginx + hysteria хуки) | ✅ dry-run ok |
| 8 | Копия бота/подписки под новые локации | ✅ задеплоено |
| 9 | Тесты 485 passed (89%) | ✅ |

---

## 1. Диагноз «высокого пинга»

- **Не сервер, а физика + оператор.** MTS (линия владельца — Новосибирск) режет ICMP до всех
  зарубежных IP (100% loss) → «пинг-тесты» в клиентах показывают ноды недоступными (в т.ч. NL —
  это был артефакт, узел `connected`). TCP-handshake оператор подделывает локально (1 мс до США
  физически невозможно) → мерить пинг по TCP-connect бессмысленно.
- Реальная задержка сквозь throttle: прямой НСК→Франкфурт ~700 мс TTFB. **Каскад через Москву**
  снимает throttle на первом (домашнем) хопе. Замер вживую с линии владельца: каскад 605/860 мс
  против прямого 698/881 мс — уже лучше даже из худшего случая (Сибирь). Для европейской РФ выигрыш
  крупнее: хоп до Москвы ~5-15 мс, Москва→Франкфурт — чистые **38.6 мс** DC-линка.

## 2. 🇩🇪 Франкфурт `166.0.28.132` (ssh `~/.ssh/denode`)

Marzban-нода **Frankfurt** (id 3). Реплика PL/NL:
- `marzban-node` docker (host-network, общий панельный `ssl_client_cert.pem`).
- nginx `reality-front` :8443 = Reality-`dest` (LE-серт `166.0.28.132.sslip.io`).
- Инбаунд **«VLESS Reality DE»** :2096 (dest `166.0.28.132:8443`, sni sslip, keypair сгенерён,
  sid `3f9c1e7a2b8d4056`) добавлен в `xray_config.json` + `nodes`/`hosts`/`inbounds` панели
  (бэкап рядом: `xray_config.json.bak-*`, `db.sqlite3.bak-*` на US-хосте).
- **Hysteria2** udp/443, systemd `hysteria-server`, **runs as root** (drop-in override — иначе не
  читает LE-серт), пароль в `/root/de-hy2-pass.txt`.
- Автопродление: `/etc/letsencrypt/renewal-hooks/{pre,post,deploy}` (стоп/старт nginx + рестарт hysteria).

## 3. 🇷🇺 Москва `130.49.143.41` (ssh `~/.ssh/runode`) — каскадный релей

Без пользователей на борту (тупой форвардер). `nft table ip cascade` (`/etc/nftables-cascade.conf`,
include в `/etc/nftables.conf`, `net.ipv4.ip_forward=1`):
```
prerouting:  tcp dport 2096 dnat to 166.0.28.132:2096 ; udp dport 443 dnat to 166.0.28.132:443
postrouting: masquerade для трафика на 166.0.28.132
```
Клиент подключается к Москве (домашний IP, без throttle), байты уходят во Франкфурт, Reality/Hy2
аутентифицируются end-to-end против Франкфурта, egress — немецкий. Проверено: оба протокола → `166.0.28.132`.

## 4. Шлюз подписки (`services/sub_gateway.py`)

- DE добавлен в `NODES` (Hy2 через `DE_HY2_PASS`).
- `build_cascade_links()` синтезирует 🇷🇺→🇩🇪 twin: копия франкфуртской ссылки, host DE→Москва,
  **sni/pbk/sid остаются Франкфурта** (иначе Reality не сойдётся через релей). Порты релея —
  `CASCADE_TCP_PORTS={2096}`, `CASCADE_UDP_PORTS={443}` (twin только для реально форвардящихся портов).
- `NODE_PRIORITY`: Каскад(0) → 🇩🇪(1) → 🇵🇱(2) → 🇳🇱(3) → 🇺🇸(4).
- Health-пробер: cascade-Hy2 наследует здоровье TCP-эндпоинта релея (RU:2096) — если Москва умрёт,
  оба каскадных варианта выпадают.
- **Деплой шлюза:** `cp /opt/vpn-bot/services/sub_gateway.py /opt/sub_gateway.py` + `restart unlock-subgw`
  (env `/etc/unlock-subgw.env`, туда добавлен `DE_HY2_PASS`). Сервис НЕ читает `/opt/vpn-bot`.

## 5. Копия бота

`bot/handlers/locations.py` (SHARED_LOCATIONS → каскад первым + рекомендация) и `buy.py` (тариф:
каскад + 🇩🇪 Германия). Задеплоено rsync + `restart unlock-vpnbot`.

## 6. Инфраструктура сейчас

| Сервер | Роль | Доступ |
|---|---|---|
| bot-VPS 23.95.3.18 | бот, БД, шлюз, Caddy | `~/.ssh/vpnbot_vps` (botvps, ControlMaster) |
| US 144.172.101.217 | **панель Marzban** + US-инбаунды | `~/.ssh/usnode` |
| PL 78.17.154.225 | нода (VLESS+Hy2+Trojan) | `~/.ssh/plnode` |
| NL 107.189.22.160 | нода (VLESS+Hy2) | shell НЕТ (awg_provision) |
| **DE 166.0.28.132** | **нода Frankfurt (VLESS :2096 + Hy2)** | `~/.ssh/denode` |
| **RU 130.49.143.41** | **каскадный DNAT-релей → DE** | `~/.ssh/runode` |

Подписка: 🇷🇺→🇩🇪 Каскад (VLESS+Hy2) → 🇩🇪 → 🇵🇱 → 🇳🇱 → 🇺🇸. Тесты 485 passed. Незапушенные коммиты
(пуш — владелец).

## 7. Открытые вопросы

1. **ufw на RU** не ставил — default FORWARD DROP сломает DNAT. Нужна `FORWARD ACCEPT` + правила, иначе не трогать.
2. **Пароли root** новых серверов светились в чате: SSH-вход по паролю отключён, но владельцу стоит
   сменить пароль в панели провайдера (консольный доступ).
3. **Для Сибири** выигрыш каскада скромнее (первый хоп НСК→Москва сам длинный). Основная выгода —
   европейская РФ. Если жалобы из-за Урала — рассмотреть азиатский/казахстанский выход.
4. **ufw на DE** тоже не ставил; порты 62050/62051 (управление нодой) открыты миру (защищены клиентским
   сертификатом). При желании ограничить их IP панели `144.172.101.217`.
5. Cloudflare-серт шлюза (из 07-23) — до октября перевести Caddy на DNS-01/Origin Cert.
