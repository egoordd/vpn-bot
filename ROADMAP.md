# UnLock VPN — Roadmap (составлен 2026-06-27)

Полный план развития бота: от быстрых правок до новых локаций и миграции.
Секреты — только по расположению (ключи в `.env`, файлы `~/.ssh/...`).

---

## Главный принцип (он диктует весь порядок)

**Мозги отдельно, трафик отдельно.**
- **Control plane** (панель, БД, выдача подписок) → можно на бот-VPS.
- **Data plane** (VPN-инбаунды: VLESS, Trojan, Hy2…) → ТОЛЬКО на отдельных
  data-нодах. Никогда на бот-VPS — IP бота привязан к платёжкам/домену/Telegram,
  спалится репутация → ляжет приём денег.

Из этого следует ключевой вывод: **самый высокорычажный следующий шаг — арендовать
одну хорошую data-ноду.** Она разом снимает 4 проблемы: ёмкость, новая локация,
просторный дом для Trojan/xhttp (без рестарта тесной US-ноды) и первая нода для
Remnawave. Поэтому она стоит первой.

---

## Текущее состояние (база)

| Сервер | Роль | Ресурсы | Запас |
|---|---|---|---|
| Бот-VPS `23.95.3.18` | бот + API + БД (control) | 2vCPU/2GB | ~1.1GB free |
| US-нода `144.172.101.217` | Marzban панель + трафик | 1vCPU/458MB | ~43MB free +786MB swap |
| NL-нода `107.189.22.160` | marzban-node (трафик), без shell | — | управляется панелью |

Протоколы: VLESS Reality (US:443, NL:2053), Hysteria2, AmneziaWG (отдельно).
Платежи: CryptoBot ✅, Tribute ✅, Robokassa ⏳заявка, CloudPayments ⏸.
Ёмкость: ~50–70 одновременных видео; цель — ~300.

---

## Порядок выполнения (TL;DR)

```
ТРЕК A (инфра, последовательно):
  A1 Новая data-нода  →  A2 Trojan на новой ноде  →  A3 Новые локации
                                                   ↘
ТРЕК B (Remnawave, параллельно):  B1 панель на бот-VPS → B2 подключить ноды
        → B3 авто-миграция юзеров → B4 переписать бота → B5 cutover

ТРЕК C (платежи, параллельно, не зависит от инфры):
  C1 Robokassa (по одобрению)   C2 Tribute: цена + ретраи доставки

ТРЕК D (обфускация, после A1/новой ноды):
  D1 CDN/Cloudflare путь   D2 xhttp (на Remnawave)   D3 Yandex-релей (белые списки)

ТРЕК E (ops/продукт, фоном): мониторинг, бэкапы, UX, рефералка
```

---

## ТРЕК A — Инфраструктура и локации

### A1. Новая data-нода ⭐ (линчпин, делаем первым)
- **Действие юзера:** арендовать сервер. Рекомендация: **2 vCPU / 4 GB / ≥2 Gbit
  unmetered**, локация под спрос (DE/FI/NL для близкого пинга к РФ, или US для
  обхода). Провайдеры: расходный VPS (не привязанный к платёжкам). Дать root SSH.
- **Claude:** провижн ноды как marzban-node (Docker, сертификат ноды из панели),
  подключить в Marzban, добавить инбаунды (VLESS Reality + Hy2), добавить host,
  внести в `NODES` шлюза + env, прогнать тесты, проверить трафик.
- **Даёт:** +ёмкость, +1 локация сразу, дом для Trojan, первая Remnawave-нода.
- Effort: M · Риск: низкий (новый сервер, прод не трогаем) · Блокер: аренда.

### A2. Trojan на новой ноде (вместо тесной US)
- На просторной ноде Trojan-WS-за-Cloudflare встаёт без риска OOM.
- **Claude:** инбаунд `Trojan WS CDN` в xray новой ноды, env-выдача
  (`"trojan":{}` в proxies + тег в inbounds), деплой шлюза. Код бота уже готов
  (ветка `feat/trojan-protocol`).
- **Блокер:** CF-токен (для CDN-пути) — от юзера.
- Effort: S · Риск: низкий · См. `deploy/trojan/README.md`.

### A3. Новые локации (повторяемый рецепт)
Каждая новая страна = повтор A1 + запись в шлюз. Шаги:
1. Арендовать расходный VPS в нужной гео, root SSH.
2. Провижн marzban-node (Docker + node cert из панели), подключить в Marzban.
3. Добавить инбаунды (VLESS Reality с серверным fp + Hy2; позже Trojan).
4. Панель → Host Settings: адрес ноды для инбаунда (remark с флагом 🇩🇪/🇫🇮…).
5. Шлюз `sub_gateway.py`: добавить хост в `NODES` (флаг, имя, hy2_host/port/pass).
6. Env: `US_HY2_PASS`/`NL_HY2_PASS`-аналог для новой ноды.
7. Деплой шлюза на US-ноду, рестарт `unlock-subgw`, тест импорта в Happ.
- **Премиум-локации:** добавить тег инбаунда в `MARZBAN_REGION_INBOUNDS` (region→
  inbounds), тариф premium роутит юзера на эту ноду.
- Effort на локацию: S–M · повторяемо.

---

## ТРЕК B — Миграция Marzban → Remnawave (большой камень)

Решено 26.06. Делается ПАРАЛЛЕЛЬНО, Marzban зарабатывает до cutover.
Даёт: HWID-лимит устройств, легче по RAM, освобождает US-ноду от панели,
открывает xhttp.

- **B1. Панель на бот-VPS:** Remnawave panel + Postgres + sub-page в Docker за
  Caddy (control-plane-only, ~1.1GB free хватает). Прод-риск ноль (параллельно).
- **B2. Подключить data-ноды:** новая нода (A1) как Remnawave-node; US/NL позже
  конвертировать/заменить.
- **B3. Авто-миграция юзеров:** официальный инструмент `remnawave/migrate`
  (docs.rw/migrate/marzban) переносит юзеров из Marzban.
- **B4. Переписать интеграцию бота:** `marzban_client.py`/`panel_client.py`/
  `panel_gateway.py`/`sub_gateway.py`, env `MARZBAN_*`→`REMNAWAVE_*`. Часть уже
  заложена (`RemnawaveClient`, `RemnawavePanelGateway`, `PANEL_PROVIDER`).
- **B5. Cutover:** `PANEL_PROVIDER=remnawave`, юзеры 1 раз переимпортируют ссылку
  (анонс!), Marzban держим как rollback, потом decommission.
- Effort: L (многодневный) · Риск: средний (cutover) · Mitigation: параллельная
  сборка + Marzban-fallback.

---

## ТРЕК C — Платежи (параллельно, не зависит от инфры)

- **C1. Robokassa:** заявка подана (самозанятый, ИНН 220454839571). Ждём
  одобрения (почта+SMS). После → ShopID + Пароль №1/№2 → интеграция: webhook на
  `https://23.95.3.18.sslip.io/...` + автовыдача (инфра HTTPS готова, повторяем
  паттерн Tribute). **Вывод — на ОТДЕЛЬНЫЙ счёт самозанятого** (риск 115-ФЗ).
  Effort: M · Блокер: внешнее одобрение.
- **C2. Tribute доводка:** (а) согласовать цену (в Tribute 100₽ vs тариф 149₽);
  (б) ретраи доставки + копия уведомления в админ-бот (был разовый сбой выдачи).
  Effort: S · Риск: низкий.
- **C3. (опц.) CloudPayments** — на паузе, вернуться при необходимости.

---

## ТРЕК D — Обфускация / обход (после новой ноды)

- **D1. CDN-путь (Cloudflare):** Trojan/VLESS-WS за `cdn.unlockvpn.site`
  (proxied). Выглядит как HTTPS к крупному CDN — пробивает блок прямых IP.
  Совмещается с A2. **Блокер: CF-токен.** Effort: S–M.
- **D2. xhttp:** самый современный CDN-транспорт Xray (лучше ws/grpc). Требует
  свежий Xray-core (≥24.11) → внедряем вместе с Remnawave (B). В рунбуке меняется
  только `network: ws→xhttp` + host. Effort: S (после B).
- **D3. Yandex-релей (белые списки мобильных)** ⭐ — единственное, что реально
  пробивает белые списки операторов (IP Яндекса пропускают). Схема: клиент → VM в
  Yandex Cloud (SNI=ya.ru) → наша нода. Аккаунт YC у юзера есть. **Решить режим:**
  аварийный канал vs всегда-вкл (влияет на egress-стоимость YC при видео).
  Effort: M · Блокер: решение по режиму.

---

## ТРЕК E — Ops и продукт (фоном)

- **E1. Мониторинг нод:** алерты на RAM/OOM/down (есть alerts-бот + watchdog) —
  убедиться, что US-нода под наблюдением (она ловила OOM). Авторестарт marzban при
  деградации.
- **E2. Бэкапы:** регулярный бэкап панели/БД (есть `marzban-backup-*.tar.gz`,
  сделать по расписанию + offsite).
- **E3. Ёмкость:** RAM нод — узкое место. Цель ~300 видео → 2–3 средние data-ноды
  ИЛИ одна крупная (4vCPU/8GB). Закрывается треком A.
- **E4. Продукт/UX:** проверить trial (3 дня), рефералку (`referral.py`), промо
  (`promo.py`), кошелёк (`wallet.py`); единый каталог тарифов на всех платёжках;
  инструкции по импорту для AmneziaWG (Happ не умеет).
- **E5. Качество:** держать тесты (сейчас 328 pass, 89%); код-ревью перед cutover.

---

## Что нужно от юзера (список решений/действий)

1. **Арендовать новую data-ноду** (2vCPU/4GB) + root SSH — разблокирует A, D, B2.
2. **CF API-токен** (Zone:DNS:Edit) — разблокирует Trojan-CDN (A2/D1).
3. **Robokassa** — дождаться одобрения, передать ShopID+пароли.
4. **Tribute** — решение по цене (100 vs 149).
5. **Yandex-релей** — выбрать режим (аварийный / всегда-вкл).
6. **Пуш коммитов** (только ключ юзера на GitHub).

## Рекомендуемый ближайший спринт
1. Юзер арендует ноду + даёт CF-токен.
2. Claude: A1 (нода) → A2 (Trojan на ней) → A3 (оформить как локацию).
3. Параллельно: C2 (Tribute-ретраи) + B1 (Remnawave-панель на бот-VPS, без риска).
4. По одобрению — C1 (Robokassa).
