# UnLock VPN — полная передача дел

**Дата:** 1 сентября 2026
**Зачем документ:** чтобы работу можно было продолжить с нуля, в другом чате или другим человеком, не имея доступа к этой переписке.

Секреты названы по расположению, значения не вписаны. Всё, что помечено ⚠️, — грабли, на которых уже спотыкались.

---

## 1. Что это за бизнес

Платный VPN для российского рынка. Продажа идёт двумя путями:

- **Telegram-бот** `@unlkvpn_bot` — покупка, продление, выдача ссылки, поддержка;
- **сайт** `unlockvpn.site` — витрина, покупка по email, личный кабинет, страница установки.

Клиент получает **одну ссылку-подписку**, вставляет её в бесплатное приложение Happ, и внутри оказываются все страны, все протоколы и два автоматических режима. Настраивать вручную нечего.

Владелец — самозанятый (Дегтярёв Егор Дмитриевич, ИНН 220454839571). На каждую оплату выписывается чек «Мой налог».

### Цифры на 1 сентября 2026

| | |
|---|---|
| Аккаунтов в панели | 94 (51 активный, 41 отключён, 2 просрочены) |
| Активных платных подписок | ~31 |
| Трафик | ~85 ГБ в сутки, ~500 ГБ в неделю |
| Расход на подписку | ~1.85 ГБ в сутки (≈56 ГБ в месяц) |
| Одновременно онлайн | ~13–15 из 50 (примерно 35%) |
| Выручка за август | 6 971 ₽ (29 оплат) |
| Рост | +29 пользователей за неделю в пике |

---

## 2. Инфраструктура

### Машины

| Роль | Адрес | Доступ | Состояние |
|---|---|---|---|
| **Бот-VPS** (мозг) | `23.95.3.18` | `~/.ssh/vpnbot_vps` | 2 ядра, 2 ГБ, load ~0.3 |
| 🇩🇪 Франкфурт | `166.0.28.132` | ⚠️ **доступа нет** | 54% трафика |
| 🇵🇱 Польша | `78.17.154.225` | `~/.ssh/plnode` | 2 ядра, 3.9 ГБ |
| 🇱🇹 Литва | `2.59.162.34` | `~/.ssh/ltnode` | 1 ядро, 961 МБ |
| 🇺🇸 США (новая) | `172.86.119.133` | `~/.ssh/usnode` | 1 ядро, 1967 МБ |
| 🇺🇸 США (старая) | `144.172.101.217` | `~/.ssh/usnode` | ⚠️ выведена, инбаундов нет |
| 🇳🇱 Нидерланды | `107.189.22.160` | ⚠️ **shell отсутствует** | работает |
| 🇷🇺 Москва (релей) | `130.49.143.41` | `~/.ssh/runode` | 1 ядро, каскады |

Хостер бот-VPS и американских машин — RackNerd (адресный пул Ace Data Centers).

### ⚠️ SSH и наш собственный VPN

**Когда Мак владельца сидит в нашем VPN, SSH ко всем нашим серверам обрывается** — соединение устанавливается и рвётся до баннера. Режут хостеры нод: это стандартная защита от того, чтобы через VPN не брутфорсили чужие серверы. Наш конфиг тут ни при чём, в нём блокируется только QUIC.

Обход зависит от сети:

- **дома** маршрут до наших серверов идёт мимо туннеля, помогает привязка к интерфейсу:
  `ssh -b $(ipconfig getifaddr en0) botvps`
- **на раздаче с телефона** в туннель уходит всё, привязка не помогает. Единственный путь — **отключить VPN**.

Свежекупленные машины (Литва, новая США) доступны и через туннель — их адреса ещё не попали под фильтры.

⚠️ sshd бот-сервера тормозит частые подключения: при серии попыток начинает отвечать `Connection closed`, и повторы делают хуже. Используйте одно соединение (`ControlMaster` уже настроен в `~/.ssh/config` для хоста `botvps`).

### Что крутится на бот-VPS

```
unlock-vpnbot   systemd  — Telegram-бот и планировщик (main.py)
unlock-api      systemd  — биллинг-API, uvicorn на 127.0.0.1:8080
unlock-subgw    systemd  — шлюз подписок, 127.0.0.1:8090, за Caddy
marzban         docker   — панель, 127.0.0.1:8000
vpn-bot-postgres-1, vpn-bot-redis-1  docker
caddy                    — TLS и маршрутизация наружу
```

Панель: `https://panel.23.95.3.18.sslip.io/dashboard/` (обязательно со слешем), логин `botapi`, пароль в `/opt/vpn-bot/.env`.
⚠️ Это учётка бота — сменить ей пароль значит сломать бота.

---

## 3. Как устроена выдача

Подписка отдаётся шлюзом `sub.unlockvpn.site`. Клиент получает **17 записей**:

```
 1. ⚡️ Авто-обход            ← балансировщик по пяти выходам
 2. 🇷🇺 Режим РУ-сервисов     ← то же + банки напрямую
 3. 🇱🇹 YouTube без рекламы   ← Литва, поставлена третьей по решению владельца
 4. 🇵🇱 Польша ✅ РУ сервисы   ┐
 5. 🇩🇪 Германия ✅ РУ сервисы ├ каскады через московский релей
 6. 🇺🇸 США ✅ РУ сервисы      ┘
 7-17. отдельные страны и протоколы
```

### Порядок задаётся в двух местах

1. **`NODE_PRIORITY`** в `services/sub_gateway.py` — порядок стран. Литва имеет `-1`, чтобы идти первой; релей `0`; дальше Польша, Германия, Нидерланды, США.
2. **Порядок инбаундов** в конфиге ядра Marzban — влияет на порядок внутри одной группы.

⚠️ Всё, чего нет в `NODE_PRIORITY`, получает значение по умолчанию и падает в середину списка. Так Литва оказалась одиннадцатой вместо третьей, а новая США всплыла выше Германии.

### Балансировщик «Авто-обход»

Стратегия `leastPing`: раз в 60 секунд опрашивает каждый выход и направляет соединение через самый быстрый. Выход медленнее 4 секунд считается непригодным.

⚠️ **`leastLoad` пробовали дважды и оба раза он оказался хуже.** Замер 1 сентября из Москвы:

| Стратегия | Куда пошло | TLS | Скорость |
|---|---|---|---|
| `leastPing` | Литва ×10 | 190 мс | 30.5 Мбит/с |
| `leastLoad` | Польша ×5, Германия ×5 | 248 мс | 24.9 Мбит/с |

`leastLoad` раскладывает по составной метрике и **самую быструю ноду не выбирает**. Переключается переменной `BALANCER_STRATEGY` в `/etc/unlock-subgw.env`. Перед разворотом — перемерять.

### Скорости нод из Москвы (замер 31 августа)

```
🇩🇪 Германия    141 мс   43.7 Мбит/с
🇱🇹 Литва       151 мс   44.9 Мбит/с   ← вровень с Германией
🇳🇱 Нидерланды  170 мс   12.7 Мбит/с
🇵🇱 Польша      224 мс   26.7 Мбит/с
🇺🇸 США         582 мс   10.7 Мбит/с
```

Перекос на Германию (54% трафика) лечится не стратегией, а появлением второй быстрой ноды. После Литвы её доля начала падать сама.

### Каскады и московский релей

Релей `130.49.143.41` делает раздельную маршрутизацию: российские адреса идут напрямую, зарубежные — на выбранный выход. Конфиг: `/usr/local/etc/xray/config.json`, копия в репозитории `deploy/relay-moscow/config.json`.

Входы: `in-pl` 2091, `in-de` 2096, `in-nl` 2092, `in-us` 2093.

⚠️ **У всех входов один общий ключ на всех клиентов.** Из-за этого каскадный трафик невозможно приписать человеку — см. раздел 8.

---

## 4. Тарифы и квота

| Тариф | Срок | Цена | ₽/мес | Трафик | Устройств |
|---|---|---|---|---|---|
| Пробник | 7 дней | 0 ₽ | — | 10 ГБ, без обновления | 1 |
| 1 месяц | 30 дней | 149 ₽ | 149 | 150 ГБ/мес | 3 |
| 3 месяца | 90 дней | 399 ₽ | 133 | 150 ГБ/мес | 3 |
| 6 месяцев | 180 дней | 699 ₽ | 116 | 150 ГБ/мес | 5 |
| 12 месяцев | 365 дней | 1 199 ₽ | 99 | 150 ГБ/мес | 5 |

Определены в `services/tariffs.py`. ⚠️ **Сайт держит собственную копию** в `web/src/lib/tariffs.ts` — её надо править синхронно, она уже дважды разъезжалась.

### Как работает квота

**150 ГБ — это месячная норма, обновляемая каждые 30 дней**, а не бак на весь срок. Год = 150 × 12 = 1800 ГБ порциями.

Обновляет **сам Marzban**: почасовая джоба `reset_user_data_usage` сбрасывает счётчик по 30-дневной границе и снимает блокировку с исчерпавших лимит. Точка отсчёта — последний сброс, иначе дата создания аккаунта. Продление обнуляет счётчик явно и переякоряет цикл на дату оплаты.

Каждый тариф несёт флаг `traffic_resets_monthly`, шлюз панели передаёт его в Marzban per-user. Умолчание в `config.py` — месячное, чтобы устаревший `.env` не мог продать одноразовый бак.

⚠️ **Лимит устройств не проверяется.** «3–5 устройств» — надпись в тарифе. Есть отчёт (раздел 8), автоматики нет.

---

## 5. Платежи и чеки

**ЮKassa** — основной путь, работает. ShopID 1396044. Вебхук `/yookassa/webhook`, защищён двумя ступенями: отметка в Redis и атомарное обновление статуса оплаты.

**Сверка брошенных корзин** (`reconcile_stale_payments`, раз в час). Провайдер отменяет брошенный счёт у себя и нам не сообщает, поэтому строка висела в «ожидании» вечно и попадала в выручку как деньги в пути — к 1 сентября накопилось 28 таких. Джоба спрашивает провайдера про всё, что старше `STALE_PAYMENT_HOURS` (3 часа), и записывает реальный ответ. Закрывает только по терминальному вердикту провайдера, поэтому счёт, который ещё можно оплатить, не трогает.

⚠️ Если провайдер отвечает **«оплачено»**, джоба строку **не закрывает**, а шлёт алерт: значит, деньги ушли, а вебхук не дошёл, и выдавать доступ должен человек. За всю историю такого не было ни разу — все 28 оказались честно брошенными.

**Tribute** — работает, вход через Telegram.

**CryptoBot** — ✅ **убран 1 сентября.** Итог за три месяца: 8 счетов, оплачен ноль. Перед удалением у CryptoBot спросили статус каждого — все истекли, никому ничего не должны.

**Баланс и кошелёк** — ✅ **убраны 1 сентября** из бота и с сайта. В реестре не было ни одного пополнения: только ручная корректировка, три промо-бонуса и списания. Настоящих денег туда никто не заносил, потому что единственный вход был через крипту.

Вместе с кошельком ушли **реферальные награды** (платили процент на баланс и не сработали ни разу) и оба экрана «пригласить друга» — они обещали процент, который платить больше нечем. **Привязка рефералов осталась**: коды, `referrer_id`, метки рекламных источников — на них держится воронка. Награды бесплатными днями — отдельная задача поверх этой привязки.

⚠️ Колонки `users.balance` и таблица `wallet_transactions` **намеренно оставлены**. Их никто не читает и не пишет, но это единственная запись о том, кто сколько держал на момент отключения: **1 000 ₽ у `edismee`, 58 ₽ у `mrsibir24`, 5 ₽ у владельца**. Удалять — отдельной миграцией, когда с этими остатками разберётесь.

### Чеки «Мой налог»

Выписывает джоба на Маке владельца: launchd `com.unlock.receipts`, раз в час, логи `~/vpn-bot/tmp/receipts_job.log`, реестр `tmp/receipts_ledger.json`.

⚠️ **Работает только когда Мак включён.** ФНС геоблочит зарубежные адреса, поэтому джоба не может жить на сервере. При росте это надо переносить на российскую машину (например, московский релей).

Два пути выписки: вебхук на сервере (сразу при оплате) и джоба на Маке (подбирает пропущенное). Оба гейтятся перепроверкой в ЮKassa.

⚠️ **Ручные чеки в приложении «Мой налог» создают доход, которого не было.** 31 августа так набежало 28 лишних чеков на 4 172 ₽ — все аннулированы. Не пользуйтесь функцией повтора чека: система выписывает их сама.

Сверка: скрипты в переписке считали UUID чеков из `payments.receipt_url` против списка `/incomes` у ФНС; лишним считается чек, на который не ссылается ни одна оплата.

---

## 6. Сайт

Next.js на Vercel, проект `unlockvpn/web`. Деплой **только из `web/`**:

```bash
cd /Users/egoordd/vpn-bot/web && npx vercel deploy --prod --yes
```

Из корня Vercel попытается собрать питон-бота и упадёт.

⚠️ Деплой иногда отвечает `Not authorized` или `fetch failed` — повторный запуск проходит.

⚠️ Переменная `NEXT_PUBLIC_SITE_URL` в Vercel указывает на **`unlockvpn.org`, домен, который не резолвится.** Вход через Telegram от неё отвязан, но канонические ссылки сайта берутся оттуда — это влияет на индексацию. Надо поправить на `https://unlockvpn.site`.

### Вход через Telegram

Работает через **редирект**, а не через официальный виджет.

⚠️ Виджет Telegram подгружает скрипт, выполняющий строки как код, и наш CSP это запрещает. Отказ был молчаливым: кнопка просто не рисовалась. Разрешать `unsafe-eval` ради одной кнопки — плохой обмен, поэтому реализован редирект: браузер уходит на `oauth.telegram.org`, возвращается на `/auth/telegram/callback` с подписью в адресе, подпись проверяется на сервере. Политика после этого стала строже: `script-src 'self' 'unsafe-inline'`, `frame-src 'none'`.

Адрес возврата берётся **из запроса**, а не из переменной окружения.

### Промокоды

Работают **только коды на выдачу подписки** (`subscription_grant`). Живые: `FREEYEAR`, `FREEMONTH`, `TEST1000`.

⚠️ **Скидочные коды (процент и сумма) не работают нигде.** Функция `apply_discount` написана, но её не вызывает ни бот, ни сайт: человек введёт код и заплатит полную цену. В рекламе не обещать.

Ввод промокода переехал из кошелька в собственный обработчик: команда **`/promo`** плюс прежняя кнопка `promo_enter`. Админская **`/gift_promo` теперь считает дни, а не рубли** (`/gift_promo КОД 30`), и отказывает на сроке, которого нет в тарифах, вместо округления. `/grant` удалена вместе с балансом.

---

## 7. Что сделано 29 августа — 1 сентября

Четырнадцать коммитов, все локальные. **Пуш делает владелец** — на GitHub только его ключ.

```
952d4e4  gateway → рабочая нода США
e4dfd90  снята регистрация удалённого обработчика
b31a743  удалена машинерия Premium (2178 строк)
6180d3b  отчёт по устройствам на одну ссылку
820a563  вход через Telegram в обход CSP
047684e  зависимости: 83 уязвимости → 14
72a8516  порог рейт-лимита 8 → 40
f409417  удалён уровень Premium
a0a6848  Литва первой в списке локаций
89172a1  промокоды на сайте и счётчик стран
92304ef  пробник 3 → 7 дней
25b0619  сайт синхронизирован по трафику
c5891b0  алерт перестал винить VPN
6cf6b2b  квота обновляется помесячно
```

### Ключевые находки

**Квота была одним баком на всю жизнь ссылки.** В проде стоял `no_reset`. Покупка на 3 месяца давала один пул на 450 ГБ, а продление меняло лимит, но не обнуляло расход: заплатив после 140 из 150 ГБ, человек покупал 10 ГБ. Один клиент был отрезан на 150.2/150. Исправлено, бэкфилл применён к 30 аккаунтам.

**Premium был витриной без товара.** Занимал переключатель на странице цен, ветку в покупке и четыре тарифа. За 112 подписок его не купил никто. Удалён целиком, вместе с 2178 строками внутренностей.

**Promo на сайте не мог сработать в принципе.** Карточка считала скидку, а все живые коды — на выдачу. Бэкенд отвечал `promo_wrong_type`, сайт превращал это в «не найден». Плюс `DEMO_USER_ID = 1` — лимиты «на пользователя» проверялись по аккаунту владельца.

**Рейт-лимит порезал бы рекламу.** 8 покупок с адреса за 10 минут при том, что за одним адресом МТС сидят тысячи абонентов. Поднято до 40.

**На Маке работал второй экземпляр бота.** Служба `com.unlock.vpnbot` с собственным планировщиком, выключенная ещё в июне. Не вредила только потому, что не могла достучаться до базы, и за два с половиной месяца написала 301 МБ ошибок. Остановлена, плист оставлен для отката.

**Миграция США.** Старая машина имела 458 МБ и постоянно теряла health-check. Новая (1967 МБ) поднята рядом, проверена живым туннелем из Москвы, записи хостов переведены, каскад на релее перенаправлен. Старая выведена: инбаунды сняты, трафика ноль.

### Ошибка, которую я допустил

Удалив `bot/handlers/locations.py`, не убрал его регистрацию в `main.py`: искал `dp.include_router`, а в коде `dispatcher.include_router`. Проверил импортом модуля — тот прошёл, потому что регистрация лежит внутри `main()`. Служба падала **148 раз**, пока я не посмотрел.

⚠️ **Импорт модуля не равен запуску процесса.** После правок `main.py` проверять запуском и смотреть `NRestarts`.

---

## 7б. Что сделано вечером 1 сентября

**Сверка платежей, которой не было.** Из 57 начатых оплат ЮKassa 20 висели в «ожидании» — некоторые с 8 июля. Первым делом спросили у ЮKassa настоящий статус каждой: **все отменены, ни одна не оплачена**, то есть выдача честная и никто не заплатил впустую. Затем добавлена часовая джоба (см. раздел 5).

**Крипта и кошелёк убраны.** Подробности в разделе 5. Ушло 3 567 строк.

**Что это открыло по конверсии.** До страницы оплаты дошли 12 живых пользователей (не считая тестов владельца) и не заплатили. Пятеро потом купили сами, **семеро — нет**, из них двое срывались на годовом за 1 199 ₽. С рекламным трафиком дыра растёт линейно, и возврат брошенных корзин — следующий очевидный шаг.

⚠️ **Замечено, не тронуто:** алиас Vercel ведёт на `unlockvpn.org`, который не резолвится (см. раздел 6), а в репозитории лежит неигнорируемый бэкап панели `backups/marzban-*.tgz` — его может случайно закоммитить `git add -A`.

---

## 8. Открытые проблемы

### Требуют владельца

**Германия недоступна.** 54% трафика на машине без SSH. Порт 22 отвечает `connection refused`, VNC у хостера не открывается. Нужен тикет в RackNerd: включить SSH и **ограничить его адресом `23.95.3.18`** (не открывать наружу), не перезагружая сервер. Публичный ключ для вставки в `/root/.ssh/authorized_keys`:

```
ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIPSOToWnxAAeDzjSyjR13J2wfJ3dhZszzQAQg5A0DZKx denode-frankfurt
```

**Включённый трафик у хостеров неизвестен.** Это настоящий потолок роста. Германия возит ~1.2 ТБ в месяц; по старой записи в её тариф включено 389 ГБ. Либо запись устарела, либо давно идёт перерасход.

**Налоговый режим.** Лимит НПД 2.4 млн ₽ в год. Тысяча подписок по 149 ₽ дают 1.79 млн — 75% потолка. Переход на ИП с УСН меняет реквизиты в оферте, ЮKassa, Tribute и чеках, готовить надо заранее.

**Бэкапы вне машины.** Сейчас лежат там же, где база. Нужно решение куда складывать.

### Требуют работы

**Каскадный трафик мимо квоты.** Релей принимает один общий ключ, байты невозможно приписать человеку. Режим «✅ РУ сервисы» не расходует 150 ГБ вообще.

*План перехода без простоя:* релей начинает принимать и общий ключ, и персональные (шлюз знает ключ пользователя, он приходит из панели вместе со ссылками); шлюз переходит на персональный; через два часа общий убирается. Плюс включить `stats`/`api`/`policy` в конфиге релея и джобу, которая складывает каскадный расход с панельным при проверке квоты в `traffic_sync`.

**Лимит устройств.** Есть `scripts/device_report.py`, автоматики нет. Запуск:

```bash
ssh botvps 'journalctl -u unlock-subgw --since "7 days ago" --no-pager' | python3 scripts/device_report.py
```

Считает не по адресам (журналов xray на нодах нет, а к Германии и Нидерландам нет доступа), а по строке клиента при обновлении подписки.

⚠️ **Две ловушки, каждая даёт неверный ответ.** Happ подмешивает в идентификатор дату: одно устройство сообщает два значения, чередуя по чётности дня — без свёртки счёт завышается в полтора раза. И мессенджеры сами открывают ссылку для предпросмотра: `TelegramBot`, `GoogleMessages`, `WhatsApp` — не устройства.

Что показала неделя: 50 аккаунтов, 99 устройств, у 27 по одному. Больше пяти — два аккаунта, один из которых наш собственный.

**Одна коробка под всем.** Бот, API, база, Redis, шлюз, Caddy и панель на одной 2-ядерной VPS. Загрузка 6%, дело не в мощности: её падение останавливает всё сразу.

**Зависимости.** Осталось 14 уязвимостей в `aiohttp`. Закрываются в 3.14, но там сменилась сигнатура `ClientResponse`, и `aioresponses` (последняя версия 0.7.9) её не понимает — падает 40 тестов, включая проверку платёжного клиента. Причина записана в `requirements.txt`.

**Спящий код.** Осталось: Remnawave (445 строк), AmneziaWG и WireGuard (439, выключены настройкой, но подключены в `connect_device.py` и `admin.py`). Кошелёк, рефералные награды и крипта удалены 1 сентября — минус 3 567 строк.

---

## 9. Как деплоить

**Прогонять тесты перед каждым деплоем:**

```bash
source .venv/bin/activate && python3 -m pytest -q
```

На 1 сентября (вечер): **569 зелёных, покрытие 88%**. Тестов стало меньше, потому что вместе с кошельком и криптой ушли и их тесты.

**Бот и API:**

```bash
scp services/*.py botvps:/opt/vpn-bot/services/ && scp bot/handlers/*.py botvps:/opt/vpn-bot/bot/handlers/
ssh botvps 'systemctl restart unlock-vpnbot unlock-api'
```

⚠️ **Удалённый модуль scp не уберёт.** Файл останется на сервере и продолжит импортироваться. После удаления — стирать руками и чистить `__pycache__`.

⚠️ **Никогда не лить `cat > /opt/...` прямо в живой файл.** Оборвётся связь на середине — файл обнулится, и служба уйдёт в цикл перезапусков. Так 2 сентября бот пролежал 14 минут (`buy.py` стал 0 строк, 39 рестартов). Правильно: во временный файл, проверить и только потом переместить.

```bash
ssh botvps 'cat > /tmp/x.py' < bot/handlers/buy.py
ssh botvps 'l=$(wc -l < /tmp/x.py); python3 -c "import ast;ast.parse(open(\"/tmp/x.py\").read())" && [ "$l" -gt 400 ] && mv /tmp/x.py /opt/vpn-bot/bot/handlers/buy.py'
```

⚠️ **И не объединять копирование с рестартом в одну длинную команду** — если оборвётся между ними, останется битый файл и работающая старая память процесса, а следующий рестарт уронит службу.

### Запасной путь к бот-серверу

Когда прямой SSH виснет (смена сети, троттлинг sshd), помогает прыжок через литовскую ноду — она доступна и с нашего VPN:

```bash
ssh -o ProxyCommand="ssh -W %h:%p -i ~/.ssh/ltnode root@2.59.162.34" -i ~/.ssh/vpnbot_vps root@23.95.3.18
```

Ключ бот-сервера остаётся на Маке: через `-W` литовская нода только пробрасывает поток, аутентификация происходит локально.

**Шлюз подписок** — ⚠️ **копировать оба файла, он читает `/opt/`, а не `/opt/vpn-bot/`:**

```bash
scp services/xray_json.py services/sub_gateway.py botvps:/opt/vpn-bot/services/
ssh botvps 'cp /opt/vpn-bot/services/xray_json.py /opt/xray_json.py && cp /opt/vpn-bot/services/sub_gateway.py /opt/sub_gateway.py && systemctl restart unlock-subgw'
```

**Сайт** — см. раздел 6.

⚠️ **Правка `.env` требует перезапуска всех трёх служб.** Дважды клиентам выдавали урезанный продукт: один раз `.env` не обновили, другой раз служба стартовала за пять часов до правки и девять дней держала старый набор в памяти.

---

## 10. Грабли

Собрано из того, на чём уже обжигались.

**Статус ноды в панели врёт.** `error` с сообщением «Failed to connect to node's API» означает, что упала проверка здоровья, а не нода. Так было со старой Америкой (возила 6.7 ГБ в сутки в статусе `error`) и с Польшей 1 сентября (контейнер работает два месяца, пинги идут с ответом 200). **Не выводить ноду из выдачи по статусу в панели** — проверять живым трафиком через `/api/nodes/usage`.

**Сканирование портов на Нидерландах бесполезно.** Их сеть принимает SYN на любой порт, включая те, где ничего нет. Проверять живым протоколом. Германия отвечает честно — там сканирование информативно.

**Не мерить из туннеля то, что находится в этом же туннеле.** Проверка Польши с машины, сидящей на польском выходе, — это проверка Польши из Польши.

**Лечение может оказаться болезнью.** Июльский keepalive добавляли против «отваливается через 20 минут» и получили ровно этот симптом. Блок QUIC снимали ради видео и сломали скорость.

**Перед разворотом прошлого решения — перемерять.** Порядок каскадов был верным на замере 4 августа и устарел к 22-му. Обоснования хранятся в комментариях и тестах именно для этого.

**Marzban сам добавляет служебные записи хостов** с подстановкой `{SERVER_IP}` при создании инбаунда. Она разворачивается в адрес панели, где ничего не слушает, и попадает в пробер как мёртвая точка. После создания инбаунда — вычищать.

**Правка конфига ядра перезапускает xray на всех нодах** и рвёт HTTP-соединение на середине запроса. Это нормально: делать с бэкапом, потом дождаться панели и проверить статусы.

**Частичный PUT в Marzban безопасен:** `proxies` и `inbounds` трогаются только когда переданы. Проверено по `crud.update_user` перед правкой 30 живых аккаунтов.

**Новый инбаунд надо вписать в `MARZBAN_DEFAULT_INBOUNDS`.** Существующим аккаунтам Marzban раздаст его сам, новые получат список без него. Сторож `check_inbound_drift` это ловит и шлёт алерт.

---

## 11. Наблюдение

```bash
# здоровье нод (пробер идёт из Москвы каждые 3 минуты)
ssh botvps 'cat /run/unlock-node-health.json'

# что реально отдаётся клиентам
ssh botvps 'journalctl -u unlock-subgw --since "6 hours ago" | grep "served token"'
# entries=17 — полный набор, entries=0 — погашенная подписка

# ошибки
ssh botvps 'journalctl -u unlock-vpnbot -u unlock-api -u unlock-subgw --since "1 hour ago" -p err'

# чеки
tail -30 ~/vpn-bot/tmp/receipts_job.log

# жив ли бот, когда SSH недоступен: 409 Conflict = опрашивает, ok:true = НЕ опрашивает
curl -s "https://api.telegram.org/bot$BOT_TOKEN/getUpdates?limit=1&timeout=0"
```

В боте: `/stats`, `/funnel`, `/find`. Сайт: `unlockvpn.site/admin`.

---

## 12. Первые пять действий на новом месте

1. **Проверить, что всё живо:** три службы активны, `unlock-node-health.json` показывает 7 из 7, подписки отдаются по 17 записей.
2. **Открыть доступ к Германии** — это 54% трафика на неуправляемой машине, первое по важности во всём документе.
3. **Узнать включённый трафик у хостеров** — без этого планирование роста остаётся арифметикой без верхней границы.
4. **Проверить чеки** — джоба на Маке, работает только когда он включён.
5. **Запушить 215 накопившихся коммитов** — они существуют только на Маке владельца.
